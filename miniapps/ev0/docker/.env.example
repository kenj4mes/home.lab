# ═══════════════════════════════════════════════════════════════════════════════
# x402 Gateway - Environment Configuration
# Copy to .env and fill in your values
# ═══════════════════════════════════════════════════════════════════════════════

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ CORE CONFIGURATION                                                          │
# └─────────────────────────────────────────────────────────────────────────────┘

# Port for the x402 gateway (default: 3000)
PORT=3402

# Environment mode
NODE_ENV=production

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ BLOCKCHAIN / WALLET                                                         │
# └─────────────────────────────────────────────────────────────────────────────┘

# Your server wallet address (receives payments)
# Generate one at: https://wallet.coinbase.com or use AgentKit
SERVER_WALLET_ADDRESS=0x...

# Private key for signing (NEVER commit this!)
# Only needed if the server needs to send transactions
# SERVER_PRIVATE_KEY=0x...

# RPC endpoint for Base mainnet
BASE_RPC_URL=https://mainnet.base.org

# Alternative RPC providers (fallback)
# ALCHEMY_API_KEY=
# INFURA_PROJECT_ID=

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ SECURITY                                                                    │
# └─────────────────────────────────────────────────────────────────────────────┘

# JWT secret for session tokens (CHANGE THIS!)
# Generate with: openssl rand -hex 32
JWT_SECRET=change-this-to-a-random-32-byte-hex-string

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ AGENT CONFIGURATION                                                         │
# └─────────────────────────────────────────────────────────────────────────────┘

# Agent wallet (MPC wallet via Coinbase AgentKit recommended)
AGENT_WALLET_ADDRESS=0x...

# Coinbase CDP API (for AgentKit)
CDP_API_KEY_NAME=
CDP_API_KEY_PRIVATE_KEY=

# Agent settings
AGENT_NAME=agent
AGENT_PERSONALITY=

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ LLM / AI CONFIGURATION                                                      │
# └─────────────────────────────────────────────────────────────────────────────┘

# OpenAI API (optional - for cloud models)
OPENAI_API_KEY=

# Ollama endpoint (for local models)
OLLAMA_HOST=http://ollama:11434

# Default model
DEFAULT_MODEL=llama3.2:latest
# DEFAULT_MODEL=gpt-4-turbo-preview

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ MEMORY / PERSISTENCE                                                        │
# └─────────────────────────────────────────────────────────────────────────────┘

# ChromaDB endpoint for vector memory
CHROMADB_HOST=http://chromadb:8000

# Memory collection name
MEMORY_COLLECTION=agent_memories

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ BROWSER                                                                     │
# └─────────────────────────────────────────────────────────────────────────────┘

# Headless browser mode
HEADLESS=true

# Browser data directory
BROWSER_DATA_DIR=/app/data/browser

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ FARCASTER (OPTIONAL)                                                        │
# └─────────────────────────────────────────────────────────────────────────────┘

# Farcaster account (optional)
FARCASTER_MNEMONIC=
FARCASTER_FID=

# Neynar API for Farcaster
NEYNAR_API_KEY=

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ MONITORING / LOGGING                                                        │
# └─────────────────────────────────────────────────────────────────────────────┘

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Structured JSON logging
JSON_LOGS=true

# Health check interval (seconds)
HEALTH_CHECK_INTERVAL=30

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ HOME.LAB INTEGRATION                                                        │
# └─────────────────────────────────────────────────────────────────────────────┘

# Traefik routing
TRAEFIK_HOST=x402.home.lab

# Enable dashboard
DASHBOARD_ENABLED=true

# Data directory for persistence
DATA_DIR=/opt/stacks/x402/data
